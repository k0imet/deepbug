# DeepBug — Python dependencies
# Core app
streamlit>=1.40
pandas>=2.0
plotly>=5.18

# AI Assistant (OpenAI-compatible chat completions)
aiohttp>=3.9
httpx>=0.27

# Misc
requests>=2.31

# --- Optional extras -------------------------------------------------
# esprima-python        : JS AST analysis (js_analyzer)
# playwright            : browser-based validators (DOM XSS, open redirect)
# aiodns, brotli        : faster aiohttp DNS/compression
